# Сетевой чат

## Описание
Сетевой чат - это многопользовательское консольное приложение для обмена текстовыми сообщениями по сети. Состоит из серверной и клиентской частей с поддержкой одновременного подключения нескольких пользователей.

## Технологический стек
1. Maven 
2. Java
3. Java Socket API
4. JUnit 
5. TCP/IP 
6. ServerSocket  
7. Socket 
8. Thread 
9. ExecutorService 
10. CachedThreadPool 
11. ConcurrentHashMap 
12. Java I/O Streams
13. BufferedReader 
14. PrintWriter 
15. File I/O


## Архитектура системы
    ┌───────────┐    ┌───────────┐    ┌───────────┐
    │ Клиент 1  │    │ Клиент 2  │    │ Клиент N  │
    │           │    │           │    │           │
    └─────┬─────┘    └─────┬─────┘    └─────┬─────┘
          │                │                │
          └────────────────┼────────────────┘
                           │
                    ┌──────┴──────┐
                    │   Сервер    │
                    │             │
                    └─────────────┘

## Потоковая архитектура сервера
*Главный поток* - ожидание новых подключений

*Потоки ClientHandler* - обработка сообщений от клиентов (по одному на клиента)

*CachedThreadPool* - пул потоков для эффективного управления ресурсами

## Потоковая архитектура клиента
*IncomingMessageHandler* - прием сообщений от сервера

*OutgoingMessageHandler* - отправка сообщений на сервер

## Настройки приложения
Файл settings.txt: `1234`
- Содержит номер порта для подключения
- Располагается в корне проекта
- По умолчанию используется порт 1234

## 🚀 Запуск приложения
### *Компиляция и сборка*
Сборка проекта
```java 
mvn clean compile
```

Запуск тестов
```java 
mvn test
```

Создание JAR файлов
```java 
mvn package
```

### *Запуск сервера*
```java
java -cp target/classes ru.server.Server
```

### *Запуск клиента*
```java
java -cp target/classes ru.client.Client
```

## 📡 Протокол обмена сообщениями
### Установка соединения
1. Клиент подключается к серверу

2. Клиент отправляет имя пользователя

3. Сервер добавляет клиента в список активных

### Формат сообщений
*Обычное сообщение:* `username: message text`

*Системное сообщение:* `username присоединился к чату`

*Команда выхода:* `/exit`

## Логирование
### Формат записей в лог-файле
```java
username [timestamp]: message text
Server [timestamp]: system message
```

### Расположение логов
- Сервер: `File.log` в корневой директории сервера
- Клиент: `File.log` в корневой директории клиента
